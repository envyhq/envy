var some_var: str

var some_other_var: bool

pub var some_other_other_var: nowt

var some_url: url {
    description = "A url to do something"
    protocol = "https"
}

module SomeModule {
    var some_module_var: int
    var some_other_module_var: float

    pub var some_module_url: url {
        description = "A url to do \"some\\thing\" else"
        protocol = "ws"
    }
}

provider Env {
    dotenv = true
}