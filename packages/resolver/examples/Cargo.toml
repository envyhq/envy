[package]
name = "nv-examples-resolver-api"
version = "0.1.0"
edition = "2021"

[dependencies]
env_logger = "0.11.2"
log = "0.4.20"
nv-parser = { path = "../../parser" }
nv-resolver = { path = "../" }
nv-provider-env = { path = "../../provider/providers/env" }
nv-provider-aws-secrets-manager = { path = "../../provider/providers/aws-secrets-manager" }
futures = "0.3.30"
